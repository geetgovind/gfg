const title = `Given an integer s,  write a program to print a square wall of size s without using string multiplication. Use nested loops instead. The * character will make up the wall.`;
const s= 3;
for(let i=1; i<=s; i++){
    for(let j=0; j<=i; j++){
        console.log();
    }
}