const title = `You are given a string s. You need to find the length of the string.`;
console.log(title);
console.log("--------------------------------------");

function strLength(str){
    return str.length;
}
const result = strLength("Geet Govind");
console.log(result);